= SimpleInbox

This project rocks and uses MIT-LICENSE.

############################INSTALL##########################################
add to routes
    resources :messages, :only => [:index, :show, :new, :create] do
        collection do
            post :mark_as
        end
    end
